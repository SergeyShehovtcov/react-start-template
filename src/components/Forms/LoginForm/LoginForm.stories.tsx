import React from 'react';
import type { Meta } from '@storybook/react';
import { LoginForm } from './LoginForm';

const meta: Meta<typeof LoginForm> = {
  title: 'Компоненты/Forms/LoginForm',
  component: LoginForm,
  tags: ['autodocs'],
};

export default meta;

export const Форма_входа_использовал_formik = {};
